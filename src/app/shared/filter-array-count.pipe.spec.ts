import { FilterArrayCountPipe } from './filter-array-count.pipe';

describe('FilterArrayCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayCountPipe();
    expect(pipe).toBeTruthy();
  });
});
